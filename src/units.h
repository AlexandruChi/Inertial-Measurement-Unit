#ifndef UNITS_H
#define UNITS_H

#define h(X) ((X) * 100)
#define k(X) ((X) * 1000)

#define kHz(X) k(X)

#define hPa(X) h(X)

#define atm(X) ((X) / 101325)

#define dC(X) ((X) - 273.15)

#endif /* UNITS_H */
